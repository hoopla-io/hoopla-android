<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:background="@drawable/bg_map_marker_ring">

        <!-- The logo bitmap is already cropped to a circle in ShopMarkerIconFactory. -->
        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/marker_logo"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center"
            android:background="@android:color/transparent"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_marker_placeholder" />

    </FrameLayout>

    <androidx.appcompat.widget.AppCompatImageView
        android:layout_width="18dp"
        android:layout_height="11dp"
        android:layout_marginTop="-3dp"
        android:src="@drawable/ic_map_marker_pointer" />

</LinearLayout>