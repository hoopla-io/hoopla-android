<resources>
    <!-- Base application theme. -->
    <style name="Theme.Hoopla" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Story viewer needs a MaterialComponents ancestor so MaterialCardView (cover ring)
         resolves its style attrs. Black window background avoids a white flash before
         the layout paints over the fullscreen black slide area. -->
    <style name="Theme.Hoopla.StoryViewer" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Full-screen dialog (e.g. gift card success). Non-floating so the
         DialogFragment window fills the screen instead of a centered card. -->
    <style name="FullScreenDialogTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>